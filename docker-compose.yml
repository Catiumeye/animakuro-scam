version: "3.8"
services:
  # animakuro-node:
  #   build:
  #     context: .
  #     dockerfile: .
  #   depends_on:
  #     - mysql-animakuro
  #     - redis-animakuro
  #   ports:
  #     - "8080:8080"
  postgresql-animakuro:
    restart: always
    image: postgres:14
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=animakurouser
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=animakurodb
    # volumes:
  redis-animakuro:
    image: redis:latest
    ports:
      - "6379:6379"
    restart: always
    # volumes:

